export type MetaData = {
  page: number,
  per_page: number,
  total_row: number,
  total_page: number,
};

export type MetaDataHealthCareStore = {
  page: number,
  size: number,
  totalData: number,
  totalPage: number,
};
