export type PoliDaftarLayananData = {
  name: string,
  id: string,
  item_id: string,
};

export type PoliDaftarFaskesData = {
  name: string,
  id: string,
  item_id: string,
  provider_type: string,
  provider_name: string,
  status: boolean,
};

export type PoliInformasiPoliData = {
  name: string,
  id: string,
  item_id: string,
};
